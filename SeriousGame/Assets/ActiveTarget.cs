using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveTarget : MonoBehaviour
{
    public bool Target = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
   /* void OnMouseOver()
    {
        if (Input.GetMouseButtonDown(0) && Target == false)
        {
            Debug.Log("Setting active target");
            Target = true;
        
        }
        else if (Input.GetMouseButtonDown(0) && Target == true)
        {
            Debug.Log("deselecting active target");
            Target = false;
        }
    }*/

}
